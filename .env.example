# 凭据 — 复制成 .env 后填好,不要提交到 git
OPENAI_API_KEY=sk-改成你自己的
OPENAI_API_BASE=https://api.omgrouter.cc//v1
MODEL=grok-4-1-fast-reasoning
GROUP=Grok官逆
# 并发上限(同时跑几章 worker)
CONCURRENCY=3
# 单章最大 tokens
MAX_TOKENS=8000
# 温度
TEMPERATURE=0.85
# 失败重试次数
MAX_RETRIES=4
